[project]
name = "xhs-monitor"
version = "0.1.0"
requires-python = ">=3.12"
dependencies = [
    "curl-cffi==0.15.0",
    "jinja2>=3.1.6",
    "loguru>=0.7.3",
    "openpyxl>=3.1.5",
    "python-dotenv>=1.2.3",
    "pyyaml>=6.0.3",
    "requests>=2.34.2",
    "retry>=0.9.2",
]

[project.scripts]
xhs-monitor = "monitor.run:main"

